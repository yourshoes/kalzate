{
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "yarn && lerna run start",
    "ci": "yarn && lerna run --parallel ci",
    "sonar": "sonar-scanner"
  },
  "dependencies": {
    "sonarqube-scanner": "^2.4.0",
    "lerna": "^3.13.1"
  }
}