{
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "yarn && lerna run start",
    "test:ci": "lerna run test:ci",
    "sonar": "sonar-scanner"
  },
  "dependencies": {
    "lerna": "^3.22.1",
    "sonarqube-scanner": "^2.4.0"
  }
}