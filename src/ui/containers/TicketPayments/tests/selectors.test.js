// import { fromJS } from 'immutable';
// import { makeSelectTicketPaymentsDomain } from '../selectors';

// const selector = makeSelectTicketPaymentsDomain();

describe('makeSelectTicketPaymentsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
