// import { fromJS } from 'immutable';
// import { makeSelectTicketTotalDomain } from '../selectors';

// const selector = makeSelectTicketTotalDomain();

describe('makeSelectTicketTotalDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
