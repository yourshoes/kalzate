// import { fromJS } from 'immutable';
// import { makeSelectTicketItemsDomain } from '../selectors';

// const selector = makeSelectTicketItemsDomain();

describe('makeSelectTicketItemsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
