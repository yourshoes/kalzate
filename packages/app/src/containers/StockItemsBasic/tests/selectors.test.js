// import { fromJS } from 'immutable';
// import { makeSelectStockItemsDomain } from '../selectors';

// const selector = makeSelectStockItemsDomain();

describe('makeSelectStockItemsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
